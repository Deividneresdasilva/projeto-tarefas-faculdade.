# Gerenciador de Tarefas

Aplicação web simples de CRUD de tarefas construída com Node.js + Express + SQLite no back-end e HTML/CSS/JS puro no front-end.

## Como executar o projeto

1. **Instalar dependências**
```bash
npm install

